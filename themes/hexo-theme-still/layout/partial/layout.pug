doctype
html(lang=config.language)
    head
        include head.pug
    body
        .wrap
            header
                a.home-link(href="/")
                    h1.title=config.title
                include nav.pug
            main.container
                block container
            footer
                block pagination
                block copyright
